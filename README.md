# ❄️ Сити Менедж Снег - Генератор Договоров

Веб-приложение для автоматического заполнения договоров с использованием Claude AI.

## 🚀 Возможности

- ✅ 5 типов договоров (Сити Менедж Гос/Озон/Перекрёсток, Скориченко Гос/Монетка)
- ✅ Загрузка файлов (PDF, изображения)
- ✅ Выбор модели AI (Haiku - быстрая, Sonnet - точная)
- ✅ История всех созданных договоров
- ✅ Защита паролем
- ✅ Автоматическая подстановка адресов из справочника Озон

## 📋 Требования

- Python 3.9+
- API ключ Claude от Anthropic

## 🛠️ Локальная установка

### 1. Установите зависимости:

```bash
pip install -r requirements.txt
```

### 2. Создайте файл `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте:
- Ваш API ключ Claude
- Пароль для входа

### 3. Запустите приложение:

```bash
python app.py
```

Откройте в браузере: `http://localhost:5000`

---

## ☁️ Деплой в облако (Render.com)

### Шаг 1: Создайте аккаунт на Render

1. Перейдите на [render.com](https://render.com)
2. Зарегистрируйтесь (можно через GitHub)

### Шаг 2: Загрузите код на GitHub

1. Создайте репозиторий на GitHub
2. Загрузите туда все файлы проекта:

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/contract-app.git
git push -u origin main
```

### Шаг 3: Создайте Web Service на Render

1. В Render нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий
3. Настройки:
   - **Name**: `city-manage-contracts` (или любое имя)
   - **Region**: Frankfurt (ближе к вам)
   - **Branch**: `main`
   - **Runtime**: Python 3
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn app:app`

4. Нажмите **"Advanced"** и добавьте переменные окружения:
   - `ANTHROPIC_API_KEY` = ваш ключ Claude
   - `APP_PASSWORD` = ваш пароль
   - `SECRET_KEY` = случайная строка (например: `jdf8h32jkdfh923jhdf`)

5. Нажмите **"Create Web Service"**

### Шаг 4: Дождитесь деплоя

Render автоматически:
- Установит зависимости
- Запустит приложение
- Выдаст ссылку типа: `https://city-manage-contracts.onrender.com`

### Шаг 5: Готово! 🎉

Откройте ссылку, введите пароль и пользуйтесь!

---

## 🔐 Безопасность

- ⚠️ **НЕ публикуйте `.env` файл в Git!**
- ⚠️ **НЕ коммитьте API ключи!**
- ✅ Используйте сложный пароль для APP_PASSWORD
- ✅ Меняйте SECRET_KEY на случайную строку

---

## 💰 Стоимость

### API Claude:
- **Haiku**: ~1.5₽ за договор (быстро)
- **Sonnet**: ~18₽ за договор (качественно)

### Hosting Render:
- **Free tier**: 750 часов/месяц (достаточно для небольшой команды)
- **Paid**: от $7/месяц (если нужно больше)

---

## 🐛 Устранение проблем

### "API Error 401"
→ Проверьте правильность API ключа в переменных окружения

### "Неверный пароль"
→ Проверьте APP_PASSWORD в .env или в настройках Render

### "Application error"
→ Проверьте логи в Render (раздел "Logs")

---

## 📞 Поддержка

Если возникли вопросы - проверьте логи приложения или создайте issue в репозитории.

---

## 📝 Лицензия

Для внутреннего использования компании Сити Менедж.
